.build/
.swiftpm/
.DS_Store
*.xcodeproj
River.app/
build/
xcuserdata/
.claude/worktrees/
.claude/settings.local.json

# Custom-dictionary A/B eval harness artifacts (local only — see DictionaryEvalTests)
ab-result.txt
*.m4a

# Transcription eval scorecards (local only — see TranscriptionEvalTests, planning 0022)
eval-*.txt

# Impeccable decision-page session logs (ephemeral)
.impeccable/questions/

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleIdentifier</key>
    <string>com.river.app</string>
    <key>CFBundleIconFile</key>
    <string>River</string>
    <key>CFBundleName</key>
    <string>River</string>
    <key>CFBundleDisplayName</key>
    <string>River</string>
    <key>CFBundleExecutable</key>
    <string>River</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>LSMinimumSystemVersion</key>
    <string>14.0</string>
    <key>LSUIElement</key>
    <true/>
    <key>NSMicrophoneUsageDescription</key>
    <string>River needs microphone access to record your voice for on-device transcription.</string>
    <key>NSHighResolutionCapable</key>
    <true/>
    <!-- Sparkle auto-update (planning 0009). The appcast is published to each
         GitHub Release by the release workflow; /releases/latest/download/ always
         resolves to the newest non-prerelease release's asset, so an installed
         copy learns about newer versions without a separate host. -->
    <key>SUFeedURL</key>
    <string>https://github.com/abgregs/river/releases/latest/download/appcast.xml</string>
    <!-- Sparkle EdDSA public key, generated once with Sparkle's `generate_keys`.
         The matching private key lives in the maintainer's keychain and the
         `SPARKLE_PRIVATE_KEY` CI secret. Every shipped build trusts only this
         key, so never rotate it casually and never commit the private key.
         See docs/architecture/release-pipeline.md. -->
    <key>SUPublicEDKey</key>
    <string>iw1MJrQRpW+dBJxlSDSLAT3WPoXRLCmcaBPZPCnSlVQ=</string>
</dict>
</plist>

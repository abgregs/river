# Planning: Current Focus

The project status log and the maintainer's queue; kept current by the maintainer and the doc-sync routine. It describes the state of the *project*, not the task of whoever is reading it — your task comes from your own issue, PR, or request.

## Status

**Identity implemented (0028)** (2026-09-17). The studies are built: the river recording indicator replaces the old HUD (`RiverIndicatorView` on a `TimelineView` + `Canvas`, every number ported from the study page into the pure `RiverIndicatorPresentation`), River's five-slat template glyph replaces the SF Symbols for the three cycle states, the vivid-turquoise accent tints Settings and onboarding, and the app ships an icon built from the same slat mark — `RecordingIndicatorView` and `LevelMeterPresentation` are deleted. Assets are generated from `Constants` by checked-in env-gated tests, with a normal test failing on drift and `make verify` asserting they reach the bundle. The maintainer's on-device smoke drove four fixes: the input level reference 0.2 → 0.08 RMS (a browser applies mic gain control, `AVAudioEngine` does not, so speech barely moved the strands), an 18 pt recentered glyph, a fixed-size panel so a toast can no longer shift the capsule, and the [0017](0017_cancel-recording.md) canceled-notice bleed — a notice no longer rides into the next recording. Feedback sounds now default **off**, matching "quiet by default" ([../design/direction.md](../design/direction.md) principle 5). Verified: `swift build` + 252 tests green, `make verify` on the signed bundle, and the running app checked against the study page. Branch `feat/identity`; PR pending.

**Identity settled; implementation queued as 0028 (2026-09-16).** Nine rounds of studies are on record in [../design/identity-studies.md](../design/identity-studies.md). Approved and parked: the five-slat microphone menu bar glyph, the river recording indicator (three strands in a capsule, lines only, with a living rest state and an ink-crest transcribing state), and the accent, vivid turquoise at OKLCH hue 196. Dropped: the app icon, until an agent can design a mark. The native build landed in [0028](0028_identity-implementation.md) on the same branch as the design record (renamed `feat/identity`). The `v0.1.0-rc1` rehearsal still waits on the maintainer's call.

**Named and identity phase queued; first release pending (0027)** (2026-09-15). PRs #22–#36 landed (#34, media pause, closed unmerged — see [0003](0003_pause-media-while-dictating.md)); the 0024/0025 specs remain a draft docs PR (#37). The name and its identity strings are on record in [0027](0027_project-naming.md); the June release candidates and `v0.1.0` were withdrawn ahead of the public launch, so **no release exists yet** — the first public `v0.1.0` is cut fresh once the current build passes its on-device smoke and the Sparkle keypair is generated. Then the design phase per [../design/direction.md](../design/direction.md).

**Automated doc sync routine shipped (0015)** (2026-07-03). Retired `.claude/skills/brief` and `.claude/skills/debrief`; rewrote contributor guidance to be harness-neutral; added `docs/conventions/doc-maintenance.md` (the routine's spec) and `.github/workflows/doc-sync.yml` (cron on the 1st and 15th, plus `workflow_dispatch`). State is tracked via the `doc-sync/cursor` branch, fast-forwarded after each completed run. PR #19 fixed an OIDC token issue (2026-07-06). See [0015](0015_automated-doc-sync.md).

**V1 shipped — `v0.1.0` released and live on Homebrew** (2026-06-22). The M11 release pipeline ran for real: [`.github/workflows/release.yml`](../../.github/workflows/release.yml) built the `v0.1.0` tag, signed with the Developer ID identity in an ephemeral keychain, notarized + stapled the app and DMG, and published `River-0.1.0.dmg` + its SHA-256 to the GitHub Release — meeting M11's exit criteria and closing the "first real run" gate in [0005](0005_release-pipeline-security.md). The Homebrew tap (`abgregs/homebrew-river`) is **live and verified**: `brew install --cask abgregs/river/river` installs the notarized build end-to-end (install → launch → onboarding → dictation). The cask's `zap` now also removes the app-specific model cache (`~/Library/Application Support/River`, ~240 MB) per [0010](0010_relocate-model-cache.md); a plain uninstall leaves it for reinstall. **With M11 done, the V1 sprint (M1–M11) is complete** — remaining work is the numbered post-V1 backlog in [_index.md](_index.md). The pre-`v0.1.0` on-device acceptance gates all passed before the tag: **0006** (all three activation modes + tap self-heal), **0010** (no Documents-folder prompt on first launch), and **0011** (insertion across native AppKit, browser, Electron, and terminal). The repo template ([../../packaging/homebrew/river.rb](../../packaging/homebrew/river.rb)) was synced to the verified live cask.

**Text insertion rewritten to keystroke injection (0011)** (2026-06-18). Replaced clipboard-paste insertion (snapshot → write → ⌘V → restore) with direct Unicode keystroke injection via `CGEvent.keyboardSetUnicodeString`, routed through the single `CGEvent.post` site. The clipboard-restore race — which on a slow first-launch paste could insert the user's *old* clipboard instead of the transcription — is now **impossible by construction**: the app never touches the clipboard. Bonus: eliminates 0006's pasteboard-transit trade-off and **retires 0007** (the markers + `savePasteboard`/`restorePasteboard`/`writePasteboard` + `clipboardRestoreDelay` are gone) — a net simplification and privacy win. Kept the insertion guard (0001) and the silent-no-op probe. Text is chunked (`Constants.keystrokeChunkUnits`, grapheme-safe) since `keyboardSetUnicodeString` is unreliable past ~20 units in some apps. Trade-off: a different, smaller risk class (auto-pairing / reactive / secure fields; IME moot for English-only V1), validated on the pre-v0.1.0 rc. Also the foundation for future append-only streaming dictation. Verified: build + 99 tests green (chunk + keystroke-count tests); on-device rc across app types is the acceptance gate. See [0011](0011_keystroke-injection.md).

**Event tap hardened to `.listenOnly` (0006)** (2026-06-17). The `CGEventTap` in `InputMonitoringCapability` is now created `options: .listenOnly` instead of `.defaultTap` — the callback only observes `.flagsChanged` and returns events unmodified, so an active (modify/consume-capable) tap was more privilege than needed; `.listenOnly` makes modification structurally impossible. The one `CGEvent.tapCreate` call site and the threading invariant are otherwise untouched. The 0006 security-review finding is now implemented (the review found no exploitable vulnerability; this is least-privilege/defense-in-depth). Verified: `swift build`/`swift test` green (the tap can't be exercised in CI — decode-helper tests are unaffected); **on-device verification rides the pre-v0.1.0 rc** — all three activation modes must still fire and the self-heal path still behave (0006 acceptance criterion). Then v0.1.0.

**Model cache relocated out of ~/Documents (0010)** (2026-06-17). The `v0.0.1-rc3` on-device test surfaced that WhisperKit's default download location (`~/Documents/huggingface`) trips a macOS Documents-folder TCC prompt and clutters the user's Documents. Fixed by passing `downloadBase` = `~/Library/Application Support/River` to the `WhisperKit` init in `TranscriptionService` (path from the new pure, unit-tested `modelDownloadBase()` helper; folder name in `Constants`). **No migration** — the folder is created fresh; reading the old `~/Documents` cache to move it would re-trigger the very prompt being removed (and pre-1.0 there's nothing to migrate). `load: true` and the single model-load site are preserved. Landing before the first public `v0.1.0` so first-run is clean. Verified: `swift build`/`swift test` green incl. the new location test; on-device "no Documents prompt on first launch" check pending by the maintainer.

**M11 public-release pipeline drafted** (2026-06-15). The release path is staged end-to-end but not yet run — it's blocked on Apple Developer Program enrollment (the Developer ID cert + notary credentials). Added: [`.github/workflows/release.yml`](../../.github/workflows/release.yml) (on `v*` tag → stamp version → ephemeral-keychain Developer-ID sign via `make verify` → notarize+staple app and DMG → publish DMG + SHA-256 → teardown on `always()`), [`scripts/make-dmg`](../../scripts/make-dmg) (dependency-free `hdiutil` packaging), and the Homebrew cask template + tap runbook under `packaging/homebrew/`. The workflow reuses the Makefile so [anti-pattern #3](../conventions/anti-patterns.md)'s bundle-integrity check runs on releases, and satisfies the [0005](0005_release-pipeline-security.md) checklist (SHA-pinned action, least-privilege `permissions`, ephemeral keychain, `Package.resolved` provenance, published checksum). **0005 Skip-button decision resolved:** the onboarding Skip button is compiled out of notarized builds via a new `RIVER_RELEASE` flag (set by the workflow through `make SWIFT_FLAGS=…`), kept for local/self-signed builds where TCC detection is unreliable. Full runbook: [../architecture/release-pipeline.md](../architecture/release-pipeline.md). Verified: `swift build`/`swift test` green in both the default and `-DRIVER_RELEASE` configurations; shell/YAML/cask drafts syntax-checked. **Not verified:** the workflow's first real run (needs the cert), which is M11's exit criteria.

**Repository protection landed** (2026-06-14). The repo was public with **no** branch protection despite git.md claiming otherwise. Added active rulesets — "Protect main branch" (PR + code-owner review via [`.github/CODEOWNERS`](../../.github/CODEOWNERS), force-push/deletion blocked, admin self-bypass) and "Protect release tags" (`v*` deletion/force-push blocked) — plus private vulnerability reporting. Enforcement verified via a throwaway PR (`REVIEW_REQUIRED`/`BLOCKED`). Closes the "once public" items in [0005](0005_release-pipeline-security.md).

**Transient pasteboard markers (0007) complete** (2026-06-12). Both pasteboard writes in `TextInsertionManager` — the transcription write and the snapshot restore — now declare the three nspasteboard.org marker types (`TransientType` / `ConcealedType` / `AutoGeneratedType`; identifier strings live in `Constants.pasteboardMarkerTypes`, mapped once to `NSPasteboard.PasteboardType` in the manager), so well-behaved clipboard managers skip recording dictations and don't re-log the restored original as a new copy. Markers are **additional types on the first item, not replacements** — the restore stays byte-faithful and the existing round-trip tests are untouched. **An empty-snapshot restore stays empty:** a restore-to-empty is a clear; a marker-only item would turn an empty clipboard non-empty for no privacy benefit. `writePasteboard` promoted to `internal` for testability (seam listed in [../conventions/tests.md](../conventions/tests.md)). This is a marking, not a guarantee — the convention is voluntary, so the README caveat is reworded, not removed, and [0006](0006_runtime-security-hardening.md)'s pasteboard trade-off now points at the markers. Verified: build + 99 tests green (3 new marker tests), plus on-device against the first marker-carrying `make install` build: dictations do not appear in Raycast's clipboard history (full empirical record, including the pre-marker ambiguity, in [0007](0007_transient-pasteboard-markers.md)).

**Custom dictionary cut from V1 (0008 removal task) complete** (2026-06-12). Per [0008](0008_custom-dictionary-redesign.md), the V1 dictionary shipped a field bug — prompt echo, where dictionary terms are hallucinated into the end of dictations — and the 224-token conditioning ceiling blocks the redesign's role packs, so the UI is cut ahead of public release. Removed: the Settings "Custom Dictionary" section (and `SettingsView`'s now-purposeless custom init), `DictionaryModel` + its test suite, and the `AppDelegate` subscription forwarding `Settings.customDictionaryTerms` into `TranscriptionService`. Removing the subscription — not just the UI — is the load-bearing half: terms a user persisted before the cut would otherwise keep biasing every dictation with no UI left to clear them. With no feed-in path, the service's term list is always empty and the echo bug is **structurally impossible**; persisted terms remain in `UserDefaults`, untouched, for the redesign. Kept: the `customDictionaryTerms` `SettingKey` (single declaration, anti-pattern #8), all `TranscriptionService` prompt plumbing (`buildPromptTokens`, the special-token filter, the empty-decode fallback, `evaluateDictionaryPrompt` + the env-gated A/B harness), and the `small.en` default. Verified: build + 96 tests green (the deleted `DictionaryModel` suite accounts for the drop from 100); zero remaining `DictionaryModel` references in code; not yet re-verified on-device.

**Focused-element paste guard (0001) complete** (2026-06-10). The synthesized ⌘V is now gated by a **read-only AX role check**: `AccessibilityCapability.classifyFocusedTarget()` reads the system-wide focused element's role/subrole and classifies it against pure editable/non-editable role tables; `TextInsertionManager.insertText` skips with `TextInsertionError.noEditableTarget` **before the pasteboard is touched** when the target is clearly non-editable, so the clipboard is never disturbed and the first-use Shift probe never fires on the skipped path. The skip surfaces as "No text field focused" through the existing `errors` publisher → menu bar. **Fail-open is structural:** unknown, missing, or app-custom roles (and the not-granted case) return `.unknown` and the paste proceeds as before, with a log line so the case is observable — only the clearly non-editable role list blocks. The pure classifier is the unit-tested core (role-table tests, no AX grant required); the AX read is the untestable leaf behind the `focusedTargetForTesting` seam (mirrors `skipPostForTesting`). The "No AX-API path" section of [../architecture/river-pipeline.md](../architecture/river-pipeline.md) now records this read-only exception — a role *read* gates the paste; AX *writes* remain rejected. Verified: build + 100 tests green, plus on-device: pastes into Notes and browser inputs unchanged; a DevTools single-click DOM selection is blocked with the menu-bar warning and an untouched clipboard (a copy made before dictation still pastes afterward); DevTools inline editing (double-click) receives the paste at the cursor as an editable context should.

**M9 (Tap modes + activation-mode picker) complete** (2026-06-09). `TapStateMachine` (pure, clock-injectable) interprets completed taps for **Single Tap** / **Double Tap**; `HotkeyManager` routes the tap modes through it while Hold stays inline. The **Activation Mode** picker ships in `SettingsView` with a mode-aware Caps Lock/Hold warning; `activationMode` persists via `SettingsStore`, which gained a small `DefaultsConvertible` protocol so `RawRepresentable` settings round-trip through `UserDefaults`. **`doubleTapWindowMs` is an internal `Constants` tunable, not a setting** — no slider, by decision (it's friction for a value the user shouldn't have to reason about). **Mid-recording reconfiguration is now live-or-deferred:** a key/mode change during a *tap*-mode recording applies **in place** — the watched key/mode is swapped while the one event tap keeps running (no rebuild, no dropped audio) — and the session posts a message on a new `notices` channel (`AppState.notice` → menu dropdown now, the recording HUD later) telling the user the newly-set key now stops the recording; **Hold** still defers until the held key is released. This also corrected a stale doc claim that a key change *rebuilds* the tap: it's always an in-place refilter, now consistent across [../architecture/threading-invariant.md](../architecture/threading-invariant.md), [../architecture/configuration.md](../architecture/configuration.md), and anti-pattern #7. Two post-review cleanups followed: the pure activation tables (`ActivationKeyOption`, `ActivationNotice`) moved to `Models/` so the session no longer reaches into `Views/` for notice copy, and deferred reconfigurations now **accumulate** (a key *and* mode change during one Hold recording both apply, not just the last). Verified: build + 92 tests green, plus on-device (all three modes, live key-change while recording, and the Caps Lock/Hold warning).

**M8 (Settings UI + custom dictionary) complete** (2026-06-08). `SettingsView` ships the Activation **key** picker (+ the verbatim Caps Lock/Hold warning from a pure, tested `ActivationKeyOption` table), the **Custom Dictionary** (list with a per-row delete button + add field), and **launch-at-login** (`SMAppService`). `@AppStorage(Settings.x.name)` binds the `Int`/`Bool` keys; the `[String]` dictionary binds through the typed `SettingsStore` via a small `@Observable` `DictionaryModel` (`@AppStorage` can't represent `[String]`). A latent bug surfaced first: `SettingsStore` never bridged external `@AppStorage` writes to its publishers (the `observation` field was declared, never wired) — fixed by observing `UserDefaults.didChangeNotification`, so a key change from the UI actually reaches `RiverSession`. The custom dictionary then exposed three more: (1) `loadModel()` lacked `load: true`, so the model never loaded at launch and `buildPromptTokens` read a nil tokenizer → an empty, inert prompt; (2) `base.en` degenerates to empty output under a prompt, so the default moved to **`small.en`** (~240 MB) — the dictionary effectively requires it (see [../requirements/custom-dictionary.md](../requirements/custom-dictionary.md)); (3) `transcribe` now falls back to an unprompted decode when a prompted run is empty, so the dictionary can only help. A gated A/B eval harness (`DictionaryEvalTests`) verified biasing ("Veed" → "Vite" on `small.en`). **Deferred:** mode picker + `doubleTapWindowMs` (M9), the model picker (M9+), pause-media ([0003](0003_pause-media-while-dictating.md)), and the model-loading indicator the `load: true` change exposed ([0004](0004_model-loading-indicator.md)). Verified: build + 72 tests green; on-device settings + transcription confirmed.

**Menu-bar visual state complete** (2026-06-06). The full cycle now does visible work end-to-end. `AppState` (`@MainActor @Observable`) bridges `RiverSession`'s Combine publishers (`state` + the new `errors`) to SwiftUI; the session imports no SwiftUI and stays the test surface. `MenuBarPresentation.visual(state:hasError:)` is a pure, unit-tested mapping to the `MenuBarExtra` icon + label — `mic` / `mic.fill` / `ellipsis`, "Ready" / "Recording..." / "Processing..." per [../requirements/core-feature.md](../requirements/core-feature.md) item 5 — with a warning glyph overriding only the idle icon. `RiverSession.errors` emits a typed `RiverError` (`.audioCapture` / `.transcription` / `.textInsertion`) per stage without blocking the return to `.idle` — the session-level error publisher M7 deferred. The message is path-redacted exactly once at the `AppState.apply(_:error:)` boundary before it reaches the menu ([ADR 0002](../decisions/0002-log-redaction-over-debug-flag.md)) and cleared when a new recording starts. See [../architecture/app-state-and-menu-bar.md](../architecture/app-state-and-menu-bar.md). Verified: build + 63 tests green, plus on-device end-to-end (menu-bar icon cycles `mic` → `mic.fill` → `ellipsis` with matching labels across a live recording cycle).

**M7 complete** (2026-06-04). Text insertion closes the cycle. `TextInsertionManager.insertText(_:)` is async: it snapshots the **full pasteboard** (every item, every type, as opaque `Data` so RTF / images / custom UTIs round-trip unchanged), writes the transcription, posts a synthesized ⌘V down/up through the capability, sleeps `Constants.clipboardRestoreDelay` (250 ms) so the target app's paste handler can pull the new contents in, then restores the snapshot. **Restore-on-throw:** any failure before the restore (a post error, or sleep cancellation) still restores the original pasteboard in the `catch` before rethrowing — stranding the transcription in the user's clipboard is a worse failure than the paste error itself. `AccessibilityCapability.postKeyEvent(...)` is now the **sole `CGEvent.post` call site** (load-bearing rule #3). It guards on `.granted`, then runs the **silent-no-op probe**: synthesizes a Shift modifier-down via `.cghidEventTap`, reads `CGEventSource.flagsState(.combinedSessionState)`, and refuses the post (downgrading `status` to `.denied`, throwing `.silentNoOp`) if the OS never reflected the synthesized modifier — the structural detector for anti-pattern #3 (a bundle that signs and trusts but silently no-ops). The result is cached in `probeConfirmed`: it runs once at the first `postKeyEvent` after `.granted`, then memoizes, and resets whenever `recheck()` sees the OS view drop below `.granted`. A CapsLock toggle pair is the documented plan-B if the Shift technique proves unreliable. `OnboardingCoordinator` is **extracted from `AppDelegate`** (closes the planning TODO): it owns the onboarding gate, the `NSWindow` lifetime, and a runtime subscription to every capability's `status` publisher that **re-presents onboarding on a `.granted → !.granted` transition** — the user-facing surface for the probe firing mid-session. `AppDelegate` is back to a thin lifecycle shell. M7 shipped a `DICTATION_VERBOSE_LOGS` build flag to gate `privacy: .public` on error strings; it was **superseded immediately after** ([ADR 0002](../decisions/0002-log-redaction-over-debug-flag.md)) by source-level path redaction. Error strings now log `privacy: .public` everywhere but pass through `LogRedaction.redactUserPaths(_:)`, which strips `/Users/<name>` paths — so failure reasons stay readable in field bug reports without leaking the account name, and user content stays protected by *omission* (only `.count` is ever logged) per [../conventions/logging.md](../conventions/logging.md). The pre-M3 `appState.errorMessage` error-surface sketch was reconciled in the pipeline doc as a **deliberate deferral, not a regression**: transient post failures were log-only at M7; the session-level error publisher landed with the menu-bar visual-state milestone (above). Verified: build + 52 tests green, plus on-device end-to-end (real Accessibility grant → hold-speak-release → transcribed text pasted at the cursor, original clipboard restored).

**M6 complete** (2026-06-03). WhisperKit 0.18.0 integrated. `TranscriptionService` exposes explicit `loadModel()` (idempotent, coalesces concurrent callers) and `transcribe(audioSamples:) -> String`; `customDictionaryTerms` is a property + setter ready for M8 to wire `Settings.customDictionaryTerms`. The load-bearing **`filterSpecialTokens(_:specialTokenBegin:)`** static helper drops timestamp / language / sentinel tokens before they corrupt `DecodingOptions.promptTokens`. `AppDelegate` fire-and-forgets `loadModel` at launch on its own Task — first run downloads ~140 MB for `base.en` to WhisperKit's cache (`~/Documents/huggingface/models/argmaxinc/whisperkit-coreml/`). `RiverSession.handleDeactivate` calls `transcribe` after `stopRecording` in a nested catch — audio and transcription failures are independent; either path still returns to `.idle` and applies pending reconfig. Typed errors: `TranscriptionError.modelNotLoaded` / `.transcriptionFailed(underlying:)` / `.emptyTranscription`. M6 deliberately keeps the explicit lifecycle (no `skipModelLoadForTesting` flag) — see [ADR 0001](../decisions/0001-defer-cycle-protocol-seams.md). Verified: build + 47 tests green, plus on-device end-to-end (Right Option → recording → transcribed-char log in `log stream`).

**M5 complete** (2026-06-02). `MicrophoneCapability` owns `AVAudioEngine` start/stop and exposes an `audioBuffers` publisher. `AudioCaptureManager` subscribes during `.recording`, waits up to 300 ms for the first buffer (engine-warmup race), then converts hardware-format buffers to 16 kHz mono Float32 via `AVAudioConverter`. `RiverSession.handleDeactivate` is async: `.recording → .processing → .idle`. The M3 deferral loop closes here too — `pendingReconfiguration` fires on every return to `.idle`.

**M4 complete** (2026-06-01). `InputMonitoringCapability` owns the system-wide `CGEventTap` on a dedicated `com.river.eventtap` background thread; the C tap callback hops to the main actor before publishing `TapEvent`s. `HotkeyManager` interprets `.flagsChanged` for the watched keycode (Hold mode only — tap modes in M9) and fires semantic `onActivate`/`onDeactivate`. Default key Right Option (keycode 61).

**M3 complete** (2026-06-01). `RiverSession.start` subscribes to `SettingsStore` publishers via `subscribeToConfiguration()`; `applyOrDeferReconfiguration` applies directly when idle and parks the change in `pendingReconfiguration` otherwise. `stop` clears the subscription set.

**M2 complete** (2026-05-29). Capability layer real, onboarding end-to-end: honest `.granted`/`.denied`/`.unknown` from non-prompting status APIs; `OnboardingView` iterates `[any Capability]` with Grant/Refresh/Skip; gate via `OnboardingGate.shouldPresent(for:)`.

**M1 complete** (2026-05-26). Architectural skeleton (Swift package, `MenuBarExtra`, `RiverSession`/`Capability`/`SettingsStore` stubs) plus the bundle → sign → install pipeline.

## Next up

**The V1 sprint (M1–M11) is complete as of `v0.1.0`** (see the top of this log). Next work is the post-V1 backlog tracked in [_index.md](_index.md).

**Transcription-quality review captured as backlog specs** (2026-07-06). A review of how to improve transcription quality landed [0022](0022_transcription-eval-harness.md) (eval harness — a local corpus + WER scorecard per model, the instrument for all model decisions) and [0023](0023_silence-decoding-hardening.md) (silence trim + `noSpeechThreshold`/temperature-fallback decoding options — kills hallucinated text from silent/accidental recordings), and rescoped [0021](0021_model-picker.md): the curated model list and the default are decided by 0022's scorecards, with the never-yet-evaluated stronger candidates (`large-v3-turbo`, `distil-large-v3`) in scope — possibly just a default bump ahead of any picker UI. **Decision on record: the [0008](0008_custom-dictionary-redesign.md) custom-dictionary redesign is dropped** — no dictionary/glossary/role-pack/personalization layers; the project uses off-the-shelf models as-is and does not modify or train them. The reserved `customDictionaryTerms` key and the `TranscriptionService` prompt plumbing remain in code with no planned consumer. See the transcription-quality grouping in [_index.md](_index.md).

**UX review captured as backlog specs** (2026-07-06). A UI/UX review of the shipped app identified the highest-impact improvements and landed them as specs 0016–0021 (sound cues, cancel recording, transient error toasts, last-transcript recovery, mic level meter, model picker), joining the already-queued 0002/0003/0004/0012. The review's headline: the app is heavily instrumented for correctness but sparse on user feedback — the first-run model-load window ([0004](0004_model-loading-indicator.md), which can silently lose audio) and the recording HUD ([0002](0002_recording-indicator-hud.md)) are the top two. See the new **Groupings** section in [_index.md](_index.md) for which items share the `AppState` seam and could land as one PR (0002 + 0018 + 0020), and the ordering dependency 0004 → 0021.

**Also queued: Recording indicator HUD** ([0002_recording-indicator-hud.md](0002_recording-indicator-hud.md)). A floating, fixed-position, non-activating "recording…" toast that observes the same `RiverState` seam — mode-agnostic (identical for Hold / tap modes), deferred but captured. Depends on the menu-bar milestone's state seam (landed). Surface the applicable conventions from `docs/` before starting.

## Working agreement

- Before any non-trivial code change: surface applicable conventions from `docs/`.
- After any non-trivial code change: doc updates are welcome, not required — drift is caught by the maintainer's automated doc sync ([doc-maintenance.md](../conventions/doc-maintenance.md)).
- Commit using conventional commits (see [../conventions/git.md](../conventions/git.md)).
- Don't push to `main` directly. PRs only.

## Notes for the agent picking this up

- The architecture docs encode hard-won lessons from a predecessor implementation. Treat the **Why:** annotations seriously — they exist because someone got bitten.
- The most common failure mode in this app's lineage is infrastructure that *looks* like it works but silently doesn't (a tap that fires but a paste that's blocked; a bundle that signs but has no identifier). Verify with `codesign -dv`, log inspection, and end-to-end tests in a real text field — not just "swift build succeeded."
- If you find a gap in the docs while working, flag it — a note in your PR is enough — rather than just plowing through. The docs are designed to evolve with the code; the doc sync picks up what a change makes stale.
